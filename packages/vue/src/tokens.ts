/**
 * Type-safe token accessor with full autocomplete
 *
 * Re-exports tokens from @ds/tokens for convenient access in Vue apps.
 */

export {
  tokens,
  colors,
  spacing,
  typography,
  borderRadius,
  shadows,
  transitions,
  zIndex,
} from '@ds/tokens';

export type {
  Tokens,
  ColorCategory,
  ColorShade,
  ColorValue,
  SpacingScale,
  BorderRadiusScale,
  ShadowScale,
  FontSizeScale,
  FontWeightScale,
  FontFamilyScale,
  LineHeightScale,
  TransitionScale,
  ZIndexScale,
} from '@ds/tokens';

