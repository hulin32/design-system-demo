{
  "name": "@ds/root",
  "version": "1.0.0",
  "private": true,
  "description": "Cross-platform design system for Flutter, React, and Vue",
  "scripts": {
    "build": "nx run-many -t build",
    "build:tokens": "nx build @ds/tokens",
    "build:react": "nx build @ds/react",
    "build:vue": "nx build @ds/vue",
    "build:affected": "nx affected -t build",
    "dev": "nx run-many -t dev --parallel",
    "lint": "nx run-many -t lint",
    "test": "nx run-many -t test",
    "storybook": "nx serve:storybook docs",
    "build:storybook": "nx build:storybook docs",
    "clean": "nx run-many -t clean && nx reset",
    "graph": "nx graph",
    "affected:graph": "nx affected:graph"
  },
  "devDependencies": {
    "@nx/storybook": "22.1.2",
    "@nx/vite": "22.1.2",
    "@nx/web": "22.1.2",
    "@vitest/ui": "^4.0.0",
    "jiti": "2.4.2",
    "nx": "22.1.2",
    "storybook": "10.1.0",
    "typescript": "^5.9.3",
    "vite": "^7.0.0",
    "vitest": "^4.0.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {}
}
